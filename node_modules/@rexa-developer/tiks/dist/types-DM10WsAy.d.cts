interface TiksTheme {
    name: string;
    baseFreq: number;
    noiseColor: 'white' | 'pink';
    oscType: OscillatorType;
    filterFreq: number;
    filterQ: number;
    attack: number;
    decay: number;
    brightness: number;
}
interface TiksOptions {
    theme?: ThemeName | TiksTheme;
    volume?: number;
    muted?: boolean;
    /** Auto-mute when the user prefers reduced motion. Default: `true`. */
    respectReducedMotion?: boolean;
    /** Minimum ms between hover() sounds. Default: `80`. `0` disables throttling. */
    hoverThrottleMs?: number;
}
type SoundGenerator = (ctx: AudioContext, dest: AudioNode, theme: TiksTheme) => void;
type ThemeName = 'soft' | 'crisp' | 'arcade' | 'glass';

export type { SoundGenerator as S, TiksOptions as T, ThemeName as a, TiksTheme as b };
